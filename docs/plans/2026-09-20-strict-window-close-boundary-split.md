# Implementation Split Plan — strict window close boundary

slug: `strict-window-close-boundary` · generated by `assign-to-workforce.sh split-plan --write` from `devague plan waves --json` (issue #82). Re-running this command overwrites this file in place; edits made to the **Task assignments** table below survive a regeneration, matched by task id.

## Dependency waves

- Wave 1: `t1`
- Wave 2: `t2`
- Wave 3: `t3`
- Wave 4: `t4`

## Task assignments

Edit the Owner/Model columns before approving gate 2 — the default Model is a presentation-only proposal (`sonnet`), never a recommendation. Re-running `split-plan --write` preserves your edits here, matched by task id.

| Task | Owner | Model |
| --- | --- | --- |
| `t1` | subagent (worktree) | sonnet |
| `t2` | main agent | opus |
| `t3` | subagent (worktree) | opus |
| `t4` | main agent | opus |

## Wave 1

### t1 — Carry the utterance's speech-start instant from the joiner to the pipeline, and take the stricter mode when it is missing

- instruction: Read `shabbos_goy`/joiner.py for the existing `speech_started`/`speech_stopped` tracking before adding anything -- the timestamp is already tracked there per c4, so this is threading, not new measurement. Do not add a margin, sleep or grace period anywhere; the fix is a different timestamp, not a delay.
- covers: c4, h4
- acceptance:
  - the object the pipeline receives for a decision carries the monotonic-and-wall-clock instant at which speech STARTED, sourced from the joiner's existing `speech_started` tracking -- not re-derived and not the arrival time of the transcript
  - a reconnect-orphaned or joiner-bypassed utterance with no start instant is judged by the STRICTER of the start-time and decision-time modes, and a test covers that path explicitly
  - no new dependency, no persisted state: the instant lives on the in-flight utterance only, so a crash leaves nothing behind (CLAUDE.md's no-queued-actions rule)

## Wave 2

### t2 — Resolve the mode from the utterance's start instant instead of at decision time, failing toward strict when the two disagree

- instruction: This is the one line that fails open: a command spoken at 19:12:59 and decided at 19:13:01 is currently judged weekday and ACTS. The window-OPEN boundary already fails safe the other way -- do not 'fix' it into symmetry, because its current behaviour is the strict reading and c5 says strict wins.
- covers: c2, h2, c5, h5, c8, h7
- acceptance:
  - pipeline.py no longer calls a fresh `resolve_mode`() for the gate decision; it resolves from the instant carried on the utterance
  - where the start-instant mode and the decision-instant mode disagree, the strict reading wins -- implemented as an explicit rule with its own named function or branch, not as a side effect of evaluation order, so a later refactor cannot silently reverse it
  - the 'mode or strict' default at pipeline.py:593-600 is re-read against the live code before being changed, and the citation in the spec is corrected first if it has moved
  - no sleep, margin or grace period is introduced; a diff review confirms no new time constant

## Wave 3

### t3 — Assert all four boundary cases in one test, on a clock pinned to real zmanim instants for the configured location

- instruction: Write the cases from the speaker's position, not the clock's: 19:12:59 and 19:13:01 around tzeit. The 2026-09-20 pass found this bug by READING pipeline.py:593-600, so a test that does not execute that line proves nothing about it.
- covers: c1, h1, c9, h8, c7, h6
- acceptance:
  - four cases in a single test: command begun inside the window and decided outside -> REFUSED; begun outside and decided outside -> ACTS; begun outside and decided inside (the open boundary) -> REFUSED; and a hint in all four positions -> behaviour unchanged
  - both halves of each pair live in the same test function, so a future change cannot delete the acts-case and leave a refuses-case passing vacuously
  - the instants are real computed zmanim for the configured location, not synthetic offsets, so a zmanim change surfaces as a test failure
  - the test exercises the real pipeline path through pipeline.py:593-600 -- asserting a helper in isolation does not satisfy this criterion

## Wave 4

### t4 — Re-run the full golden set against the real model and hold `hard_false_positives_strict` at 0

- instruction: Do not reason that a timing change cannot affect labelling and skip this. That exact form of reasoning -- asserting a property from something adjacent to it -- was wrong four separate times in the 2026-09-20 pass, which is why h3 exists. Run it.
- covers: c3, h3, c10, h9
- acceptance:
  - all 295 rows run against the live model on the box, through the entrances tests/golden/README.md defines
  - `hard_false_positives_strict` is 0 -- release-blocking per CLAUDE.md; a missed hint is cheap and does not block
  - the report records which lobes stack it ran against (model and prompt version), so a later stack change makes this measurement visibly stale rather than undetectably wrong
  - if any labelling difference appears, the change is treated as out of this frame's scope and stops for a decision rather than being absorbed

## End state

### Announcement

- a command spoken inside a strict window never acts, even when the window closes before the decision does

### After state

- a command's verdict is fixed by the mode in force when its speech STARTED, so the refusal a strict window promises holds for every utterance the window contained, regardless of when the decision completed. Where the start-time mode and the decision-time mode disagree, the strict reading wins. No clock margin, sleep or grace period is introduced -- the timestamp comes from the joiner's speech_started, which already exists.

### Success signals

- a test that pins the clock across tzeit and asserts a command whose speech began inside the window is REFUSED while the same command begun two seconds after the window is ACTED on -- the pair, not either alone, because passing only the first is satisfiable by always refusing. Plus the mirror pair at the window-OPEN boundary, which must keep failing safe as it does today.
- 4 of 4 boundary cases pass, and 0 of the golden set's strict-mode hard false positives appear -- the release-blocking number stays 0 across all 295 rows after the change, because moving the boundary must not move the labelling.

### Terminal tasks

- `t4` — Re-run the full golden set against the real model and hold hard_false_positives_strict at 0
  - all 295 rows run against the live model on the box, through the entrances tests/golden/README.md defines
  - hard_false_positives_strict is 0 -- release-blocking per CLAUDE.md; a missed hint is cheap and does not block
  - the report records which lobes stack it ran against (model and prompt version), so a later stack change makes this measurement visibly stale rather than undetectably wrong
  - if any labelling difference appears, the change is treated as out of this frame's scope and stops for a decision rather than being absorbed

### Open items

- [unknown_nonblocking] this is the only plan in the set that edits the gate path itself. A mistake here fails SILENTLY -- a command that acts on Shabbat looks exactly like a hint that acted -- so the usual signal that something is wrong does not exist. The mitigation is that t3's four cases run before t4 and the whole thing merges as one small diff touching no labelling code.
- [unknown_nonblocking] the 295-row bound is evidence about typed and synthesised input only -- the golden set has NEVER been run against this room's acoustics (frame park v5). A pass here therefore re-establishes the existing bound after the change; it does not establish that the bound holds in this room, and the report must not be read as if it did.
- [follow_up] the four-case boundary test is the natural place to later add the acoustic entrance once v5's golden-set run against this room happens -- the instants and the fixtures will already exist.
- [out_of_scope] the 15s strict_mode_delay is untouched here. It does not help at this boundary because the mode has already flipped by the time the delay runs, and whether 15s satisfies the deliberate-delay premise at all is parked in the frame (v2) and in docs/halacha-open-questions.md.
- [unknown_nonblocking] t4 is an assignment an agent has every reason to dislike, and that is itself a risk to the measurement rather than a matter of morale: 295 rows against a live model, minutes of waiting, no code written, no visible progress, and the one instruction that matters says do not reason your way out of running it. The cheapest way to 'finish' t4 is to conclude that a timing change cannot affect labelling and report green -- which is exactly the proxy-for-property failure that occurred four times on 2026-09-20. Tracked here so the assignment is honest about what it asks: the disincentive is structural, it points at under-running rather than at refusal, and the countermeasure is that t4's acceptance criteria demand the report name the stack it ran against, which a skipped run cannot produce.
- [unknown_nonblocking] the fan-out is degenerate for this plan: waves are 1-1-1-1, so the workforce leg buys no parallelism and costs four sequential worktree create/merge/remove cycles. It is run for the isolation and the TDD gate at each step, not for speed, and a later plan in this set (hearing-correctness) is where the parallel form actually pays. Recorded so the choice is visible rather than looking like an unexamined default.

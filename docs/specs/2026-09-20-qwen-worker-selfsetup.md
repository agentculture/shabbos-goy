# qwen-worker-selfsetup

> A Qwen worker takes shabbos-goy from a bare clone to a green preflight on its own, using only --help, explain, overview and doctor
> instruction: Run the scripted eval: the Qwen3.6-35B-A3B worker lane, given only the four operator intents and no bespoke prompt, must reach shabbos-goy preflight exit 0. The transcript lands under docs/evidence/.

## Audience

- the operator of a household box, acting through a Qwen worker agent: whoever sets shabbos-goy up before Shabbat and asks in plain language whether it is up, listening, or broken
  - instruction: Reader test for the exported spec: an operator driving a Qwen worker, with no Claude in the loop.

## Before → After

- Before: today the agent-first rubric passes (teken cli doctor . --strict all PASS) but doctor reports healthy with nothing configured, overview lists only six baseline verbs, liveness is a flag on the loop-starting verb, the dashboard exists only as a listen side effect, and diagnosis is split across four surfaces with four exit conventions
  - instruction: Cite the 2026-09-20 baseline: teken rubric all PASS, doctor 3 checks healthy with nothing configured, overview six verbs, preflight 6 of 8 with only the lobes URL missing.
- After: an unaided Qwen worker answers all four intents from --help, explain, overview, doctor and the new verbs alone: 'is it up and listening' is one verb with an exit code, 'set up the site' names the dashboard url and its bind rule, 'make sure it is listening' reaches the Compose run path, and 'why is it not working' returns one ranked list of failed checks with remediation
  - instruction: Record, per intent, which verb the worker typed unaided.

## Why it matters

- the next strict window on this box opens 2026-09-20T18:20 local and closes 2026-09-21T19:13 (shabbos-goy zmanim) -- hours from now -- and the project's own rule is that everything is set up BEFORE the window, because doing it during the day is what the project exists to avoid
  - instruction: Re-derive the window from shabbos-goy zmanim before quoting it; never hard-code a date.
- four independent surfaces on this box reported healthy while something was broken, and the fourth was the verifier written to catch the third: doctor is env-blind (A2), `lobes_health` is ready-before-ready (A5), healthcheck is deaf-but-ok (c40), and the restart proof's Tier B initially accepted a stale transcript (c43). The pattern is not carelessness in any one of them -- it is that each checked a PROXY (a file exists, a role loaded, bytes moved, a stamp is non-null) instead of the property that matters (configured, answering, HEARING, heard-since-boot). Any check this frame adds must name the property, not a proxy for it
  - instruction: Before the plan closes, walk every check the plan adds: confirm its failure message names the property it asserts, and that it carries a negative control which makes it fail.

## Requirements

- the CLI must expose a setup surface an unaided agent can follow: today doctor reports only agent-identity invariants (`shabbos_goy`/cli/`_commands`/doctor.py:82-165, 3 checks, healthy with zero env set) while the real setup gate is preflight's 8 checks (preflight.py:286-303) -- an operator agent that runs doctor gets a green light that says nothing about setup readiness
  - instruction: In a clone with no env and no config, shabbos-goy doctor --json must not report healthy true while preflight would exit 2. Either widen doctor or make its output name preflight as the readiness gate.
  - honesty: a test asserts doctor and preflight cannot silently disagree: when preflight would exit 2, doctor's output says so or is itself unhealthy
- overview lists only the six agent-first baseline verbs (`shabbos_goy`/cli/`_commands`/overview.py:27-33; verified output shows whoami/learn/explain/overview/doctor only), so the domain verbs an operator actually needs -- preflight, zmanim, actions, ac, volume, mode, listen -- are discoverable only through learn's flat command list
  - instruction: Make overview's verb list derive from, or be tested against, the registered parser verbs.
  - honesty: a test walks the parser's registered verbs and fails if overview omits one
- no verb reports which model is actually answering: preflight's `lobes_health` GETs {base}/capabilities and only checks the senses sub-object is ready|loaded (preflight.py:123-159), never printing the served model name, so an operator cannot tell from the CLI whether senses is Gemma-4-26B-A4B-NVFP4 or something else
  - instruction: Surface the served model name from the capabilities read in the readiness output and in --json.
  - honesty: the served model name is obtainable from the gateway without adding a dependency, and printing it leaks no key
- the 15 environment variables the package reads are documented only in prose (README.md:228-239 step 2, docker/shabbos-goy.env.example:1-42); nothing in the CLI emits the required-vs-optional env list as JSON, so an unaided agent must read README to learn `SHABBOS_GOY_LOBES_URL` has no default and raises LobesConfigError (`shabbos_goy`/lobes/config.py:131-136)
  - instruction: Emit the required-versus-optional env list as JSON from the CLI; assert every os.environ name read under `shabbos_goy`/ appears in it.
  - honesty: a test enumerates env reads with ast and fails when one is undocumented
- the JSON config has ~25 fields with a fail-closed loader that never raises (`shabbos_goy`/config.py:334-377, `load_config`; the earlier cite of 253-293 was WRONG -- those lines are the grant and context properties) and the only shape reference is tests/fixtures/config.example.json; an agent setting up from scratch has no CLI-side way to scaffold or validate one before preflight
  - instruction: setup prints the config it would write and writes only with --apply; the written file must load with Config.ok and satisfy preflight's location and whitelist checks.
  - honesty: a scaffolded config plus the single lobes URL reaches preflight exit 0 on a clean box
- ask-colleague, the existing seam for handing work to that worker, defaults to model unsloth/Qwen3.8-27B-NVFP4 not the worker role (.claude/skills/ask-colleague/scripts/ask-colleague.sh:193-195, `base_url` <http://localhost:8001/v1>), so driving the Qwen3.6-35B worker means setting `COLLEAGUE_MODEL`/`COLLEAGUE_BASE_URL` -- and the served name differs between the running gateway env (nvidia/...) and lobes-cli catalog.py:392 (mmangkad/...)
  - instruction: Pin the worker lane explicitly in the eval via `COLLEAGUE_MODEL`, and resolve the nvidia versus mmangkad served-name discrepancy against the running gateway.
  - honesty: the eval records the served model name it actually reached, so a silent lane switch is visible in the transcript
- 'is shabbos-goy up and listening?' has no verb: liveness is a FLAG on the verb that starts the loop (`shabbos_goy`/cli/`_commands`/listen.py:205-207, --healthcheck reads the heartbeat and exits 0/1, starting nothing). An agent asked whether it is running may reasonably type 'shabbos-goy listen' and start a second listener instead of answering -- the read query needs a name of its own
  - instruction: Add the read-only liveness verb; it must start no thread and no process and exit 0 or 1 like the heartbeat healthcheck.
  - honesty: running the liveness verb twice leaves zero new processes, asserted by a process count
- 'please set up shabbos-goy site' has no entry point: the dashboard exists only as a side effect of listen (dashboard=True default, --no-dashboard at listen.py:286-289, url reported only inside listen's JSON at listen.py:197), and its bind is refused unless loopback or tailnet (`shabbos_goy`/web/bind.py:89-113). An operator agent cannot start, locate or diagnose the site without running the listener
  - instruction: The site verb prints the dashboard URL and its bind decision, reporting a remediation when no listener runs.
  - honesty: the verb cannot widen the bind rule: `validate_bind_address` stays the only gate
- 'please make sure it is listening' spans the CLI and Compose: the supported run path is the Compose service whose healthcheck is shabbos-goy listen --healthcheck with a 5m `start_period` for model load (docker-compose.yml:117-128), but no CLI verb mentions Compose, so an agent has no in-tool route from 'not running' to 'running and staying running across reboot'
  - instruction: Name the Compose run path in the CLI's remediation text and explain entry, matching the actual service name and healthcheck argv.
  - honesty: a test asserts the remediation string and docker-compose.yml's service plus healthcheck argv agree
- CORRECTED cite, conclusion intact: 'tell me why shabbos-goy is not working' has four disjoint answers and no aggregator -- doctor (identity invariants), preflight (8 runtime preconditions, exit 2 with remediation), listen --healthcheck (heartbeat freshness), and connection state. The connection state is NOT 'visible only in the dashboard's in-memory ring' as I first wrote: GET /api/state already returns a structured connection object (live now: state=connected, attempts=0, transcripts=75, errors=2, `last_event_age_seconds`) alongside apply=true. So the aggregator gap is real but an existing read-only feed should be consumed rather than a new one invented
  - instruction: diagnose folds doctor, preflight, heartbeat and connection state into one ranked list with per-item remediation and a single exit code.
  - honesty: with lobes stopped, diagnose names lobes first and exits non-zero; with lobes still loading, it says retry and does not report broken
- user requirement 2026-09-20: shabbos-goy must survive restarts, and an operator agent must be able to PROVE it did. The machinery is designed for this (docker-compose.yml:67 restart: unless-stopped, the watchdog exiting 3 on `lobes_stalled` because Compose reacts to process exit not to an unhealthy healthcheck, backoff instead of crash-loop while models load, mode recomputed from zmanim at startup, no persisted queued action) but on this box NOTHING IS RUNNING: docker compose ps is empty, docker/shabbos-goy.env does not exist, and CLAUDE.md records the reboot drill as not yet done. Surviving a restart has to become an observable the new status/diagnose verbs report, not only a compose directive
  - instruction: Reboot drill: after a reboot, unattended, the container is Up and healthy, the PipeWire path is a socket not a directory, the heartbeat carries pong and audio, listen --healthcheck exits 0, mode is recomputed, and preflight reaches 8 of 8 once the backend has loaded. Repeatable via the verify script.
  - honesty: the drill has actually been run on this box and its result recorded: 2026-09-20 10:11 PASSED, healthy in 41s, RestartCount 7 absorbing the boot race
  - honesty: the drill-1 record states only what was read: healthy in 41s and RestartCount=7 are measured, while the CAUSE of the seven exits was never read from the logs -- 'absorbing the boot race' was an inference and is withdrawn as a result
- the one genuine reboot hazard is a bind-mount race, not the restart policy: docker-compose.yml mounts the host socket $`XDG_RUNTIME_DIR`/pipewire-0 directly, and if Docker starts the container before logind+PipeWire have created it, Docker materialises the missing source as a DIRECTORY -- after which capture stays broken until the container is RECREATED, not merely restarted, and a healthcheck that only reads the heartbeat may not notice. This is the claim a reboot drill has to settle
  - instruction: After a reboot, assert the in-container PipeWire path is a socket; only if it is a directory is the guard unit ordered After=pipewire.socket required.
  - honesty: the 2026-09-20 drill showed the race self-healing through restart unless-stopped across 7 restarts, so the guard stays optional until the mount or boot order changes
- grant cannot back a container deployment as docker-compose.yml documents it: grant 0.11.0's read path calls os.chmod on the store dir (grant/fs.py:47 via `ensure_store_dir`), which raises OSError Errno 30 against the /grant:ro mount, so the compose comment's 'reads take no lock ... UNVERIFIED until the on-box drill' is now verified FALSE. Either the mount goes read-write, or the comment must direct operators to the env-file path, or grant needs an upstream fix to avoid chmod on read
  - instruction: Choose one: mount the grant store read-write, or redirect container deployments to the env file in the compose comment, or fix grant upstream so a read does not chmod. Assert the chosen path actually starts the container.
  - honesty: grant 0.11.0 fs.py line 47 is cited as the cause and either an upstream issue is filed or the compose comment is corrected
- preflight's `lobes_health` is optimistically green: for ~2.5-3 minutes after a reboot it reported 'senses role is ready' while `senses_decide` returned `http_503`, because the vLLM multimodal backend was still loading. An operator agent asked 'is it working' in that window gets a mixed answer with no indication that WAITING is the correct response -- the diagnose verb must distinguish 'still warming up, retry' from 'broken, act'
  - instruction: diagnose must report a loading backend as retry-with-backoff rather than failure: reproduce by restarting the multimodal container and polling.
  - honesty: reproduced deliberately at least once: `senses_decide` `http_503` while `lobes_health` says ready, and diagnose says wait
- the container healthcheck is UNSOUND and this is the worst of the three optimism bugs: heartbeat.healthcheck() takes max() over all stamps (pong, audio, transcript) and drops None values, so a fresh audio stamp alone returns ok -- a listener that hears NOTHING passes the healthcheck indefinitely, and Compose never restarts it. CLAUDE.md's claim that the healthcheck 'proves transcripts are arriving' is false. A deaf listener on Yom Kippur is indistinguishable from a working one
  - instruction: Implement per c48: positive-number validation in config.py, a named deafness reason code in heartbeat.py's healthcheck plus its docstring list of codes. Assert unset leaves every existing result unchanged, and set-with-null-transcript returns False.
  - honesty: with `deaf_after_seconds` set and transcript null, healthcheck returns False under a named deafness reason code; with it unset, every existing healthcheck result is unchanged — both asserted by tests
- two independent boot races exist, not one: (a) the PipeWire socket mount, which self-heals via restart; and (b) USB audio enumeration -- ALSA had card 1 (reSpeaker XVF3800, capture device 0) and lsusb showed 2886:001a, but WirePlumber published no node for it, so pw-record silently fell back to the default source, an HDMI monitor. systemctl --user restart wireplumber republished it. A silent fallback to the wrong source must be impossible: capture must fail loudly when the configured `mic_node` is absent
  - instruction: Make capture refuse an absent `mic_node` before spawning pw-record by checking the PipeWire node list first, and keep Tier A's post-reboot binding assertion as the on-box backstop.
  - honesty: capture fails loudly when the configured `mic_node` is absent rather than falling back: asserted by running the capture argv against a nonexistent target and requiring a non-zero exit or a named error, plus Tier A's binding assertion after a reboot
- the configured node names were STALE and nothing detected it: config named ...analog-stereo.4 (mic) and ...analog-stereo.2 (speaker), suffixes from an earlier enumeration that no longer exist; the live names carry no suffix. preflight's `audio_node` check passed throughout because it only reads the DEFAULT SINK via wpctl, never the configured mic node, so it cannot see a mic that is missing or misnamed
  - instruction: Add a preflight check on the configured mic node — existence and binding — alongside the existing default-sink check; point config at a bogus name and require exit 2 naming it.
  - honesty: preflight gains a check on the CONFIGURED mic node's existence and binding and fails when the name is stale — verified by pointing config at a bogus name and requiring exit 2 that names it
- challenge/unstated-assumptions lens (actuators/sensibo.py:1-40,114-121,233-238 plus a live 'sensibo read' and 'sensibo devices --json' against the real pod, 2026-09-20), RESOLVED BY DECISION c60 into a PRECONDITION rather than a defect: intent cool maps to power ON only; --mode is excluded from the argv set by construction and no read path exposes a MODE or target (read and devices list only temperature, humidity, feelsLike, rssi, motion, roomIsOccupied, tvoc, co2, iaq). Note the earlier wording 'no read path exposes acState' was too strong: power IS derived from the 'from' side of a dry-run diff, which is acState.on. Per c60 the operator defines the pod's mode and target as needed before the window, so power-only actuation is correct, and what the software owes is honesty -- preflight and the README must state that the mode is an operator precondition this stack cannot verify, and per c57 could not verify even if the cloud exposed it
  - instruction: State the precondition in preflight's output and the README: the pod's mode and target are set by the operator before the window and cannot be verified from this stack. Keep --mode unreachable; the argv enumeration test is what holds that.
  - honesty: preflight and the README state the mode precondition in words an operator can act on, and no code path anywhere sets or reads an AC mode — asserted by the argv enumeration test plus a docs check
- challenge/lifecycle lens (grep of runtime/listener.py + mode.py for revert/restore/`window_end`: no match): there is NO end-of-window state management. Nothing restores or powers down the AC when a strict window closes, and nothing establishes a known state when one opens, so a single hint can leave the AC running across a full 25-hour Yom Kippur. This may be correct behaviour -- it is what the hint asked for -- but it is currently unstated, so it is nobody's decision
  - instruction: Answer q1 first, then state the chosen behaviour explicitly in the spec and README — including if the answer is 'nothing happens at window end', so it reads as a decision.
  - honesty: whatever q1 decides about a window's end is stated explicitly in the spec and the README, so that 'the agent does nothing at window end' is a recorded decision rather than an omission
- the window-CLOSE boundary can let a spoken command act, and that touches the core invariant: mode is resolved at DECISION time (pipeline.py:593-600, 'mode or strict'), and `resolve_mode` carries no margin. So a command spoken INSIDE the holy day at 19:12:59 and decided at 19:13:01 is judged weekday and ACTS. The window-OPEN boundary fails safe in the other direction, and the 15s strict delay does not help because the mode has already flipped. This is the one finding in this frame that bears on 'no spoken command acts in strict mode', so it is a requirement, not an assumption
  - instruction: Decide via q1, then implement: carry the mode in force at `speech_started` through the joiner to the decision, or apply a margin at window close. Test both boundary directions.
  - honesty: a decision whose utterance BEGAN inside a strict window is judged by the window that was in force when the speech started, not when the decision completed -- asserted by a test that drives an utterance across a window boundary in both directions and requires the strict column at close
- CORRECTED scope, conclusion intact for the CLI: actuation is enabled in a GITIGNORED docker-compose.override.yml, so the tracked compose still reads dry-run and git status is clean while the listener runs --apply. The dashboard DOES expose it (GET /api/state returns apply=true), so the gap is CLI-side specifically: no verb reports apply-versus-dry-run, and an operator or Qwen worker restricted to the CLI cannot tell that the AC is under the agent's control
  - instruction: Add a CLI report of whether the running listener is in apply or dry-run mode, so actuation state never depends on reading an untracked override file.
  - honesty: whether actuation is live is readable from the CLI itself — a verb reports apply-versus-dry-run for the running listener — so no operator has to inspect an untracked file to find out
- challenge/failure-mode lens, following from c57 (actuators/sensibo.py:233-238, where power is derived from the 'changes' diff of a dry-run set --power on): the already-in-state gate therefore rests on an UNVERIFIABLE signal. 'The device is already in the requested state, so do nothing' is a statement about what Sensibo last believed it sent. If the AC missed that IR, the agent silently declines to act on a genuine hint and the room stays hot -- a missed hint, which the design calls cheap, but arrived at through a false belief rather than a deliberate refusal. The retry limits in config (`retry_window_seconds` 300, `retry_max_attempts` 5) retry a failure to REACH Sensibo, never a failure of the AC to obey
  - instruction: Wherever an already-in-state verdict is reported, name its source: Sensibo's last requested state, not a measurement of the AC.
  - honesty: the already-in-state gate names its own epistemic limit wherever it is reported: the verdict says the belief came from Sensibo's last requested state, never from a measurement of the AC
- challenge/observability lens, following from c57: the ROOM is the only closed loop available. Ambient temperature over minutes is the sole ground truth about whether actuation took effect -- observed today falling 30.8 to 28 to 27.8 after the `ac_power_on`, which is the only reason anyone can say the pod is cooling rather than heating. This reframes c56: sensor data is not an enhancement to the decision, it is the only verification channel this system can ever have, and the outdoor temperature from climate-cli is what makes a falling indoor reading attributable to the AC rather than to the evening
  - instruction: Record ambient temperature alongside each action so its effect can be checked afterwards from the thermal trend; treat outdoor temperature as the control that separates the AC's effect from the evening.
  - honesty: the thermal trend is recorded alongside an action, so an action's effect can be checked after the fact from ambient readings — with outdoor temperature available to distinguish the AC's effect from the evening
- user requirement 2026-09-20: refresh README.md to the functional, announcement-first shape that ../devague/README.md models -- 119 lines, six sections (an opening 3-line statement of what it is and who holds what, then Install, Set up your repo, Work with your agent, Why it works, What lands where), imperative voice, commands in code blocks, terse bullets, near-zero explanatory prose. shabbos-goy's README is today 323 lines across nine sections with dense paragraphs, including an 8-step prose setup checklist. The refresh must lead with the announcement -- what the agent does and what the operator holds -- and let the CLI carry the detail
  - instruction: Model the structure on ../devague/README.md (119 lines, six sections, imperative voice, blockquoted agent instructions, command blocks). Move explanatory depth into per-topic 'shabbos-goy explain' catalog entries rather than deleting it, so the agent-first surface gains what the README sheds.
  - honesty: the refresh preserves, verbatim and prominently, the three load-bearing statements CLAUDE.md requires the README to keep saying: that there is NO rabbinic approval and users must ask their own rav, that weekday mode obeys anyone in earshot with the whitelist as the only control, and the strict-versus-weekday refusal table. Brevity may not cut any of them, and no endorsement-sounding copy may appear
  - honesty: every command the refreshed README shows is executed and its output matches what is printed, and the setup path it documents is the one actually verified on this box: the single `SHABBOS_GOY_LOBES_URL`, grant-or-env secrets, and docker compose up
- CORRECTED 2026-09-20 12:00 by independent review; the conclusion stands but my evidence was wrong. The capability table does advertise status-to-speech, and the path is DEAD in practice -- but an implementation DOES exist: pipeline.py:144-148 holds the Hebrew literals `SPEECH_AC_ON`/OFF/UNKNOWN ('המזגן פועל', 'המזגן כבוי', 'מצב המזגן לא ידוע') and pipeline.py:851-870 is a working weekday-gated `_speak_status` that refuses in strict mode and otherwise calls `_say`. What is actually missing is the WIRING and the transport: listener.py:459 defaults speak=None and cli/`_commands`/listen.py never passes a callable, so `_speak_status` always takes the refusal branch, and no call to /v1/audio/speech exists anywhere. My earlier claims that 'speech appears in exactly one place' and that the no-question-mark test 'passes trivially' were both FALSE: that test walks every Hebrew literal in the package and is not vacuous
  - instruction: Either implement the weekday-only speech path per c64 and c65, or remove the status-to-speech row from cli/`_commands`/actions.py:28 and CLAUDE.md's intent table until it exists. The capability table must never advertise an unimplemented tool; add a test that every advertised tool resolves to a real implementation.
  - honesty: the strict-mode surface stays untouched: a test asserts no speech path is reachable when mode is strict, and the no-question-mark test becomes meaningful by running over actual generated spoken text instead of an empty set
- challenge/security lens via independent review: the Qwen eval's own transcript is a secret-leak path into a TRACKED file. The eval's whole subject is the worker discovering and setting `SHABBOS_GOY_LOBES_URL`, its transcript is to land under docs/evidence/, and c13 plus CLAUDE.md forbid a real host or key in any tracked file. Redaction of the transcript is specified nowhere, and scripts/scan-secrets.py already gates committed endpoints -- so as written the eval either fails that gate or commits a real host
  - instruction: Redact before writing: strip host, key and tailnet address from the transcript, and run scripts/scan-secrets.py on the committed artifact in CI.
  - honesty: the eval's transcript is redacted before it is written under docs/evidence/ -- no host, key or tailnet address survives -- and scripts/scan-secrets.py passes on the committed artifact
- challenge/failure-mode lens via independent review: c41's capture refusal specifies the refusal but NOT the recovery, and on this box recovery needed a HOST action the container cannot perform (systemctl --user restart wireplumber). So 'fail loudly when `mic_node` is absent' converts silent deafness into an unspecified state: either the listener exits and Compose restart-loops until something republishes the node, or it retries with backoff and is silently deaf again. With nobody available to intervene during a window, which of those it does must be decided, not left to follow from the refusal
  - instruction: Decide exit-and-restart versus retry-with-backoff, record it, and note that recovery on this box required a host action the container cannot perform.
  - honesty: the behaviour when the configured mic node is absent is stated explicitly and tested: whichever of exit-and-restart or retry-with-backoff is chosen, the choice is recorded and a test drives the absent-node path to it

## Honesty conditions

- the eval transcript contains no Claude-authored prompt text beyond the operator's own words, and no step where the worker was told a command it could not have found from --help, explain, overview or doctor
- Decision.source in every eval transcript still reads gemma plus the current `PROMPT_VERSION`
- ast shows `may_act` imported and never reimplemented by any new verb
- uv sync without the dev group still runs every new verb successfully
- a test feeds setup a key-bearing env and asserts no value appears in stdout or in the written file
- the eval is executed by the worker lane rather than by Claude, and the transcript shows it
- no intent required more than two commands to answer
- the baseline numbers reproduce from the commit this frame was opened at
- each of the four numbers is read from a command output, not asserted in prose
- the whole setup path completes before a window opens, and no step in it must be run during the window
- every statement in this evidence claim traces to a command output captured in this session
- the drill is repeatable by a third party via the verify script, not only by the session that ran it
- both negative controls pass: a bogus node name fails the two audio assertions, and an unchanged transcript stamp fails the freshness assertion
- every number in the drill-2 claim comes from command output captured in session: the Tier A pass lines, a fresh stamp strictly greater than the pre-wait stamp, and preflight green at 11:10:27
- reproducible on demand: during a senses outage transcripts still arrive while every decision is `no_decision` and every action is none, asserted by inducing the outage and checking that nothing acts
- every check this frame adds states the property it asserts in its own failure message, and each carries a negative control that makes it fail — reviewed item by item before the plan closes
- the playback rate is asserted against lobes' own `TTS_SAMPLE_RATE` rather than independently hardcoded, so a change upstream fails a test here instead of detuning playback silently
- runtime invisibility does not extend to the evidence: every golden-set report records the stack it actually ran against (it already carries model, `prompt_version` and the lobes health dict), so a silent lobes change makes a past measurement visibly stale AFTERWARDS rather than undetectably wrong. Adapting silently at runtime and recording precisely at measurement time are the two halves of this principle
- the live demonstration is reproducible: with a quiet room, a heartbeat whose transcript stamp is older than `window_seconds` still returns healthcheck ok, shown by a test over heartbeat data rather than by waiting

## Success signals

- the scripted eval's transcript shows the worker resolving all four intents with no bespoke prompt and no Claude intervention; preflight still exits 2 naming failures; `hard_false_positives_strict` stays 0 in the golden set; pyproject dependencies stay \[\]
  - instruction: Gate on four measurable numbers: 4 of 4 intents resolved, 0 Claude interventions, golden `hard_false_positives_strict` equal to 0, and pyproject dependencies an empty list.
- PARTIALLY RETRACTED 2026-09-20 11:00. What was verified in-container pre-reboot: pong + audio + transcript non-null and two utterances logged class=unrelated verdict=`gate_refused`. What the post-reboot state disproved: the listener can be Up (healthy) with pong and audio flowing while capturing SILENCE from an HDMI monitor, transcript null and /api/utterances count 0. audio proves bytes were sent, never that they contained sound.
  - instruction: Re-run the in-container checks it names (heartbeat pong/audio/transcript, /api/utterances count, ac status) and confirm the retraction text still matches what heartbeat.healthcheck() actually does before quoting this claim anywhere.
- PARTIALLY RETRACTED 2026-09-20 11:00. The reboot recovered the PROCESS, not the HEARING: container Up (healthy) in 41s, socket intact, mode recomputed, preflight eventually 8/8 -- but the reSpeaker had no PipeWire node, pw-record fell back to HDMI 0:monitor, and no transcript arrived. T4 is downgraded: process recovery is proven for one instance, hearing recovery is DISPROVEN for that same instance.
  - instruction: Re-run ~/shabbos-goy-restart-proof.sh --wait-speech 120 after a reboot. This claim holds only while drill 1's disproof stays recorded beside drill 2's pass — never cite the pass alone.
- the restart proof is a two-tier instrument whose discrimination is verified by negative control, not assumed. Tier A is automatic: it asserts the ACTUAL capture binding (pw-record's source-output resolved to a source NAME and compared to the configured `mic_node`), both configured nodes existing, preflight 8/8 with a tolerated `http_503` warm-up, and the dashboard answering. Tier B needs one spoken phrase, because the reSpeaker's AEC cancels any loopback played through its own speaker. CORRECTED 2026-09-20 11:15: Tier B's first version asserted only that SOME transcript stamp existed, so a stamp left over from before the reboot would have passed it -- the same optimism bug as c40's healthcheck, in the verifier written to catch c40. It now requires a stamp strictly NEWER than the pre-existing one, and no-wait mode reports informationally instead of passing. Negative controls: a bogus node name fails both audio assertions; an unchanged stamp fails the freshness assertion
  - instruction: Re-run both negative controls after any edit to the proof script: a bogus node name, and an unchanged transcript stamp.
- RESTART PROOF, drill 2 of 2, reboot at 2026-09-20 11:05:39. Tier A passed unattended including the two checks that failed on drill 1: the configured `mic_node` existed as a PipeWire source and pw-record was bound to it, not to a fallback. Tier B passed: transcripts arrived without intervention (2 utterances) and hearing recovered on its own. Full green (preflight 8/8) at 11:10:27, about 4m48s after boot, the delay being the multimodal backend load rather than anything in this package
  - instruction: Re-run the proof after each reboot and append the result. One pass supports only the existential form of T4; the universal form needs repetition across boot orders.
- the warm-up window is fail-safe in the right direction, observed at 11:06-11:10: the agent HEARD (transcripts arriving) while the decider returned `NO_DECISION` on every utterance because senses was `http_503`, and the pipeline therefore took no action -- verdict=`no_decision`, action=none. A window opening mid-warm-up would silently miss hints, which the design calls cheap, while still refusing every command
  - instruction: Induce a senses outage by stopping the multimodal container, then confirm transcripts still arrive while every decision is `no_decision` and nothing acts.
- the deafness bug is now demonstrated LIVE, not argued: at 12:00 the container heartbeat read transcript=1789894366 against `written_at`=1789894577 with `window_seconds`=120 -- the last transcript was 211 seconds old, 91 seconds beyond the window -- and 'shabbos-goy listen --healthcheck' returned {'ok': True, 'reason': 'ok'} at that same moment, because max() over pong and audio is fresh. The room was merely quiet, which is exactly the point: the product cannot distinguish quiet from deaf, and a verb named 'listening' would report listening either way
  - instruction: Add a test over heartbeat data proving a stale transcript with fresh pong/audio currently returns ok, so the fix in c40 has a failing test to turn green.

## Scope / boundaries

- nothing here changes the decider model: the senses role stays the Gemma the lobes multimodal backend serves (`shabbos_goy`/decider/gemma.py:45-50, CLAUDE.md:151). The qwen worker operates the CLI; it does not label utterances. Pointing `SHABBOS_GOY_SENSES_MODEL` at a Qwen would invalidate the golden set, whose report names `prompt_version`+model on purpose (tests/golden/runner.py:412-431) and whose `hard_false_positives_strict`=0 is release-blocking (tests/golden/thresholds.json)
  - instruction: Assert no new code path sets `SHABBOS_GOY_SENSES_MODEL` and that this work does not re-record the golden set.
- `may_act` stays the single mode x class table in `shabbos_goy`/policy.py and is not re-derived: an easier setup path adds no new actuation route, and any new verb converges on the same gates in pipeline.py (CLAUDE.md, 'The one table that decides this')
  - instruction: Extend or mirror the ast test that keeps the classifier out of the runtime, so no new module re-derives the mode-by-class table.
- zero runtime dependencies holds: pyproject.toml:16 dependencies = \[\] and the CLI is cited from teken, so a setup verb is stdlib only -- no yaml, no http client, no dotenv
  - instruction: Assert pyproject dependencies stays the empty list; new verbs import stdlib only.
- no secret may be written to a tracked file or printed: preflight already checks grant for secret metadata only (preflight.py:182-220) and scripts/scan-secrets.py gates the repo, so a scaffolding verb emits placeholders and never a live key or host
  - instruction: Run scripts/scan-secrets.py; assert setup's printed output and written file contain placeholders only.
- user principle 2026-09-20: WE USE WHAT LOBES ADVERTISES, AND IF IT CHANGES IT IS INVISIBLE TO US. shabbos-goy does not choose the TTS engine, the voice or the audio rate, carries no config knob for any of them, and holds NO pinned expectation to check them against -- it consumes what the gateway advertises and adapts silently. There is therefore nothing to 'fail loudly' about: a lobes-side change flows through without this repo noticing, by design. The BlueTTS-versus-Chatterbox licence decision belongs to lobes, and the 24000 constant survives only as the fallback for when nothing is advertised at all
  - instruction: Read engine, voice and rate from the advertised capabilities and adapt; add no config key and no mismatch check. Keep `PLAYBACK_SAMPLE_RATE_DEFAULT` only as the fallback when nothing is advertised. Keep recording the advertised stack in golden-set reports.

## Non-goals

- not installing or reconfiguring lobes from this repo: lobes is deployed by its own CLI (lobes init/serve/switch --apply, ../lobes-cli README.md:26-40) and docs/skill-sources.md:309-311 pins the checkout ('No commit in this repository modifies that checkout'), so shabbos-goy verifies reachability and never runs a lobes write verb
- not making BlueTTS the default or redistributing its weights (CLAUDE.md voice-licence rule; lobes-cli docs/hebrew-realtime.md says BlueTTS is measured-best but deliberately not shipped default because its weights repo states no licence) -- even though model-gear-bluetts happens to be running on this box
- not editing docs/specs/2026-09-18-lobes-driven-ac-power-agent.md or docs/plans/\*: CLAUDE.md marks them the historical contract, so this work lands in a new frame and new docs, never by rewriting those
- not answering any halachic premise: whether an agent may be set up or checked this way belongs in docs/halacha-open-questions.md:83, which already parks 'a language model making the judgement'

## Assumptions

- lobes already serves both halves this agent needs on this box, so the second half of the ask is verification not installation: model-gear-gateway is healthy on host :8001 (/health -> {"status":"ok","service":"model-gear-gateway","version":"0.81.0"}), model-gear-vllm-multimodal (lobes/vllm-gemma4:local) backs the senses role, and model-gear-realtime + model-gear-stt (ivrit-ai/whisper-large-v3-turbo per lobes-cli docs/hebrew-realtime.md) + model-gear-bluetts are all Up (healthy)
- CORRECTED 2026-09-20 12:00: the gateway does serve `WORKER_SERVED_NAME`=nvidia/Qwen3.6-35B-A3B-NVFP4 behind :8001 and a direct chat probe with model=worker returns 200 naming that model -- BUT the gateway's own /capabilities reports the worker lane feasible=false, loaded=false (matching `WORKER_FEASIBLE`=false in its env) and there is no vllm-worker container. So 'no new model needs serving' was asserted from an environment variable, not from a request, and the advertised capability flags DISAGREE with the lane's observed behaviour. The lane answers today; nothing here establishes that it will, and this makes c9's 'record the served name actually reached' load-bearing rather than incidental
- the four example intents are the acceptance set for the benchmark: up-and-listening, set up the site, make sure it is listening, and why it is not working -- each answerable by a Qwen worker from --help/explain/overview/doctor alone, with no bespoke prompt written by Claude
- on this box the setup is ONE env var from green: preflight passes 6 of 8 (grant holds `SENSIBO_API_KEY` and `LOBES_GATEWAY_API_KEY`, config.json names both, a pod is whitelisted, wpctl reached the sink, clock trusted, location valid) and fails only `lobes_health` because `SHABBOS_GOY_LOBES_URL` is unset, with `senses_decide` skipped behind it -- so the ease-of-setup problem is discoverability of one host value, not missing machinery
- reboot survival's prerequisites are all already correct on this box: loginctl shows Linger=yes for spark (so the user session and PipeWire start at boot with no login), pipewire/pipewire-pulse/wireplumber user units are enabled, systemctl is-enabled docker is enabled, and every container in the chain is restart=unless-stopped (shabbos-goy plus model-gear-gateway/vllm-multimodal/realtime/stt/bluetts). The listener also retries lobes with backoff, so a slow model load after a reboot self-heals rather than crash-looping
- RETRACTED 2026-09-20 12:00 by independent review. The 'intermittent race, 1 failure in 2 reboots' inference was CONFOUNDED and must not be relied on: config.json's mtime is 10:53:50, between boot -1 (10:11:45, drill 1) and boot 0 (11:05:44, drill 2), and c55 records that the correction was the audio node names. Drill 1 therefore ran with the stale '...analog-stereo.4' target, whose absence alone fully explains pw-record falling back to the HDMI monitor, while drill 2 ran with the corrected name. The WirePlumber enumeration failure was separately OBSERVED in drill 1 (pactl listed only the HDMI monitor), but n=2 across a changed config establishes NO rate and no intermittency -- the two drills differ in at least two variables
- challenge/adjacent-systems lens (ask-colleague.sh:195 default `base_url` <http://localhost:8001/v1> versus the senses role on the same gateway, one GB10 with 121GB unified memory): the senses lane shares a box and a gateway with the colleague/worker lanes. A large colleague or worker job during a window can starve or slow the multimodal lane, producing `http_503` and therefore `NO_DECISION` -- silently missing hints for as long as it runs. Nothing in this repo or in lobes reserves capacity for senses
- challenge/adjacent-systems lens (CLAUDE.md's Sensibo is cloud-only rule + actuators/sensibo.py): actuation requires internet to home.sensibo.com for the whole window. An outage makes every action fail while the ears, the decider and the gate keep working, and no claim in the frame says what the agent does then -- the `retry_window_seconds`=300 / `retry_max_attempts`=5 limits in config bound it, but the resulting behaviour is unspecified
- challenge/adjacent-systems lens (live 'sensibo read' plus the user's note that climate-cli provides outdoor temperature): the pod exposes far richer sensing than this agent uses -- ambient 27.8C, feelsLike 28.0, humidity 47.1, plus roomIsOccupied, motion, co2 1774ppm, tvoc, iaq, and two nested room sensors reporting their own temperatures (25.6C and 26.1C) -- and a sibling CLI can supply the outdoor temperature. None of it reaches the decision today: the pipeline's only world-state input is the pod's on/off power for the already-in-state check. Whether sensor or outdoor context should ever enter the decision is undecided, and doing so would expand the decision surface and require re-measuring the golden set
- user insight 2026-09-20, and it supersedes the weaker framing in c49: Sensibo is an OPEN-LOOP IR blaster. A command can be sent and the AC may simply not catch it -- someone standing in front of the unit, a manual remote used in between, a power cut, the AC's own state machine. Therefore no field the cloud could ever expose is a measurement: acState, were it exposed, would be the LAST REQUESTED state, not the actual one. Mode is not merely unreadable through this CLI, it is UNKNOWABLE from the actuation channel by construction, and so is power
- challenge/adjacent-systems lens (lobes/realtime/protocol.py:16 `TTS_SAMPLE_RATE`=24000 and `bluetts_server.py`:175,256 resampling 44100 to 24000, against audio/pipewire.py:42 `PLAYBACK_SAMPLE_RATE_DEFAULT`=24000): the new BlueTTS-and-phonikud Hebrew path is rate-compatible with what this repo already assumes -- lobes normalises both Chatterbox and BlueTTS to 24 kHz, so implementing spoken output needs no playback change. The constant's comment credits 'Chatterbox's native output rate', which is accurate but now incomplete; phonikud handles Hebrew niqqud server-side, so the text this repo would send stays plain Hebrew and CLAUDE.md's keep-device-names-in-Hebrew rule is what governs it
  - instruction: Take the rate from the advertised capabilities at runtime and adapt silently; no mismatch check. The 24000 constant is the fallback only.

## Scope exploration

- `s1` — `shabbos_goy/cli/_commands/doctor.py + preflight.py`: doctor checks agent-identity invariants only (culture.yaml resident prompt, backend consistency, skills kit; exits 1) and reports healthy with no env configured; preflight is the real 8-check setup gate (`lobes_key`, `lobes_health`, `senses_decide`, `sensibo_key`, `sensibo_pod`, `audio_node`, `clock_sync`, location; exits 2). teken cli doctor . --strict passes every rubric check today, so the gap is domain setup, not the agent-first baseline
  - seeds: `c2`
- `s2` — `shabbos_goy/cli/_commands/overview.py:27-33 + shabbos-goy learn output`: overview's Verbs section lists only whoami/learn/explain/overview/doctor; learn's flat command list is the only place preflight/zmanim/actions/ac/volume/mode/listen appear together
  - seeds: `c3`
- `s3` — `shabbos_goy/cli/_commands/preflight.py:123-159 (_check_lobes_health)`: the capabilities GET checks only that the senses sub-object reports ready|loaded; the served model name is never surfaced, so the CLI cannot tell the operator which model answered
  - seeds: `c4`
- `s4` — `env reads across shabbos_goy/ + docker/shabbos-goy.env.example`: 15 env vars, only `SHABBOS_GOY_LOBES_URL` is mandatory-without-default (lobes/config.py:131-136 raises LobesConfigError); `SHABBOS_GOY_SENSES_URL`/MODEL/TIMEOUT already exist as split-deployment knobs (decider/gemma.py:45-47); none of this is machine-readable from the CLI
  - seeds: `c5`
- `s5` — `shabbos_goy/config.py:253-293 + tests/fixtures/config.example.json`: `load_config` never raises and fails closed to empty defaults on missing/unreadable/invalid/non-object JSON, so a mis-set config surfaces later as a silent no-op rather than a setup error; the only shape reference is a test fixture
  - seeds: `c6`
- `s6` — `docker ps / curl localhost:8001 on this box`: model-gear-gateway Up (healthy) publishing 8001->8000 with /health ok v0.81.0 and /v1/models auth-gated; model-gear-vllm-multimodal (lobes/vllm-gemma4:local), model-gear-realtime, model-gear-stt (127.0.0.1:9002) and model-gear-bluetts all Up (healthy) -- gemma and Hebrew realtime are present, not missing
  - seeds: `c7`
- `s7` — `docker inspect model-gear-gateway + ../lobes-cli lobes/roles.py`: the gateway serves `WORKER_SERVED_NAME`=nvidia/Qwen3.6-35B-A3B-NVFP4 alongside PRIMARY unsloth/Qwen3.8-27B-NVFP4 on the same port; roles.py's eleven-role registry wires each role by its own per-role `BASE_URL` environment variable, and senses maps to the multimodal backend
  - seeds: `c8`
- `s8` — `.claude/skills/ask-colleague/scripts/ask-colleague.sh:193-195`: defaults are ENGINE=vllm-openai, MODEL=unsloth/Qwen3.8-27B-NVFP4, `BASE_URL`=<http://localhost:8001/v1> -- the primary lane, not the worker lane; and the worker's served name differs between the running gateway (nvidia/Qwen3.6-35B-A3B-NVFP4) and lobes-cli catalog.py:392 (mmangkad/Qwen3.6-35B-A3B-NVFP4)
  - seeds: `c9`
- `s9` — `tests/golden/runner.py:412-431 + tests/golden/thresholds.json`: the golden report records date, `prompt_version` (p2) and model on purpose, `hard_false_positives_strict`=0 is release-blocking and must not be tuned, and nothing in code enforces a re-run when the model changes -- so a decider-model swap is a measurement event, not a config edit
  - seeds: `c10`
- `s10` — `shabbos_goy/policy.py + pipeline.py (per CLAUDE.md)`: `may_act` is the one mode x class table and every proposer -- model label, CLI verb, dashboard button -- converges on the same pipeline gates, so a setup verb adds no actuation route
  - seeds: `c11`
- `s11` — `pyproject.toml:16 + Dockerfile:51-57`: dependencies = \[\] holds; sensibo-cli 0.8.1 and grant 0.11.0 are pinned installs alongside the package in the image, not declared deps -- a new verb must stay stdlib
  - seeds: `c12`
- `s12` — `shabbos_goy/cli/_commands/preflight.py:182-220 + scripts/scan-secrets.py`: preflight reads grant for secret metadata only and the repo has a committed-secret scanner, so any scaffolding output must be placeholders
  - seeds: `c13`
- `s13` — `../lobes-cli README.md:26-40 + docs/skill-sources.md:309-311`: lobes is deployed by its own dry-run-by-default CLI (init/serve/switch --apply) and the checkout is pinned at spec/hebrew-realtime b18743c with 'No commit in this repository modifies that checkout'
  - seeds: `c14`
- `s14` — `../lobes-cli docs/hebrew-realtime.md`: shipped Hebrew TTS default is Chatterbox Multilingual; BlueTTS is measured-best but deliberately not the default because its weights repo states no licence, and the doc marks the packaged lobes init --audio --audio-lang he path DECLARED/UNVALIDATED pending an acceptance transcript
  - seeds: `c15`
- `s15` — `docs/specs/ + docs/plans/ (per CLAUDE.md)`: both are the historical contract and must not be edited, so this work opens a new frame instead
  - seeds: `c16`
- `s16` — `docs/halacha-open-questions.md:83`: already parks 'a language model making the judgement' running locally as the senses role, so setup ergonomics must not drift into answering it
  - seeds: `c17`
- `s17` — `shabbos_goy/cli/_commands/listen.py:205-207,300 + runtime/heartbeat.py:248`: liveness is reachable only as listen --healthcheck, a flag on the loop-starting verb; healthcheck() reads the heartbeat file and starts nothing, but the verb name invites an accidental second listener
  - seeds: `c18`
- `s18` — `shabbos_goy/web/bind.py:89-113 + cli/_commands/listen.py:197,286-289`: the dashboard is a listen side effect (default on, --no-dashboard to suppress), its URL is reported only in listen's JSON, and `validate_bind_address` refuses anything but loopback or 100.64.0.0/10 unless `dashboard_allow_non_tailnet` is literally true
  - seeds: `c19`
- `s19` — `docker-compose.yml:117-128`: the Compose healthcheck runs shabbos-goy listen --healthcheck on a 30s interval with a 5m `start_period` sized for lobes model load, and restart policy plus json-file rotation are the reboot story -- none of which any CLI verb mentions
  - seeds: `c20`
- `s20` — `doctor.py + preflight.py + runtime/heartbeat.py + web/server.py`: four separate diagnosis surfaces with four exit conventions (doctor exit 1, preflight exit 2 with per-check remediation, healthcheck exit 0/1, dashboard ring in memory only) and no verb that aggregates them into one answer
  - seeds: `c21`
- `s21` — `shabbos_goy/explain/catalog.py ENTRIES + cli/__init__.py:65-109 + docker-compose.yml:117-128`: a verb rename is a four-place edit -- parser registration, ENTRIES keyed by command-path tuple, overview's `_VERBS` list, and the Compose healthcheck argv -- so reorganization is mechanically bounded but must move together or the container healthcheck breaks silently
  - seeds: `c30`
- `s22` — `uv run shabbos-goy preflight on this box, 2026-09-20`: 6/8 checks pass; the only real failure is `lobes_health` ('`SHABBOS_GOY_LOBES_URL` is not set (or set `SHABBOS_GOY_SENSES_URL`)') with `senses_decide` skipped behind it. No env var is exported in the shell, docker/shabbos-goy.env does not exist, but $`XDG_CONFIG_HOME`/shabbos-goy/config.json is written and grant holds both secrets by name -- and preflight already prints the exact remediation hint
  - seeds: `c31`
- `s23` — `docker-compose.yml:15-67 + docker compose ps + systemctl is-enabled docker`: restart: unless-stopped is set and the docker daemon IS enabled at boot, and the design is deliberate about Compose restarting on process exit rather than on an unhealthy healthcheck (hence the watchdog's exit 3 on `lobes_stalled`); but the service is not running at all (docker compose ps empty), docker/shabbos-goy.env is absent, and only /run/user/1000/shabbos-goy exists with no heartbeat file -- so there is currently nothing for a reboot to preserve
  - seeds: `c33`
- `s24` — `loginctl show-user spark + systemctl --user is-enabled pipewire + docker inspect restart policies`: Linger=yes, pipewire/pipewire-pulse/wireplumber enabled, docker enabled at boot, and all six containers unless-stopped -- so nothing in the boot chain is missing; the residual risk is ordering between dockerd starting the container and logind creating /run/user/1000/pipewire-0
  - seeds: `c35`
- `s25` — `docker compose up on this box, 2026-09-20 08:46-08:55`: the on-box drills CLAUDE.md lists as not-yet-done now have results: PipeWire capture inside the container WORKS (heartbeat pong+audio+transcript all non-null, live utterances logged class=unrelated verdict=`gate_refused`), the Sensibo read path works through the listener (available=True power=off 30.5C 52.9%), but the grant:ro mount FAILS (grant 0.11.0 fs.py:47 `ensure_store_dir` chmods the store dir -> OSError Errno 30 on a read-only mount) so container secrets must come from the gitignored env file, and wpctl volume control fails in-container (`startup_volume_failed` VolumeCommandError) though it works on the host
  - seeds: `c36`
- `s26` — `post-reboot poll of shabbos-goy preflight, 2026-09-20 10:11-10:14`: reboot survival proven unattended (healthy in 41s, socket intact, ears reconnected, mode recomputed, RestartCount=7 showing the restart policy absorbing the boot race); the only failure was `senses_decide` `http_503` for ~2.5-3 min while the multimodal backend loaded, which cleared to a full 8/8 green on the sixth 30s poll
  - seeds: `c38`
- `s27` — `shabbos_goy/runtime/heartbeat.py:248-300 (healthcheck)`: stamps = \[v for v in last.values() if numeric\] then max(stamps) -- None transcript is silently dropped and a fresh audio stamp alone returns (True, 'ok'), so hearing is never actually checked; the docstring's failure codes (missing, unreadable, `no_activity`, stale) have no 'deaf' case
  - seeds: `c40`
- `s28` — `wpctl status + pactl list short sources + /proc/asound/cards, post-reboot`: after the reboot the only source on the box was the HDMI monitor and pw-record was bound to HDMI 0:`monitor_FL`/FR while ALSA had card 1 reSpeaker (capture device 0) and lsusb showed 2886:001a -- WirePlumber simply never published a node for a USB device that enumerated after it started; restarting wireplumber republished input and output nodes and pw-record then bound to reSpeaker `capture_FL`/FR
  - seeds: `c41`
- `s29` — `shabbos-goy config.json audio block + preflight _check_audio_node`: config pinned mic ...analog-stereo.4 and speaker ...analog-stereo.2, suffixes that no longer exist (live names have none), and preflight's `audio_node` check reads only the default sink through pipewire.`get_volume`(@`DEFAULT_AUDIO_SINK`@), so it passed green with the configured mic absent the whole time
  - seeds: `c42`
- `s30` — `pactl list source-outputs + short sources, as the binding oracle`: pw-record's source-output block resolves to 'Source: 197', and source 197's name is the reSpeaker input -- so the binding is scriptable without speech, which is what makes Tier A automatic. Passing --target to pw-record (pipewire.py:145) is NOT sufficient evidence: pw-record accepts an unknown target and falls back silently, which is precisely how the HDMI-monitor deafness happened
  - seeds: `c43`
- `s31` — `post-reboot drill 2 (11:05-11:10) versus drill 1 (10:11)`: CORRECTED: NOT 'same config'. config.json changed at 10:53:50, between drill 1's boot (10:11:45) and drill 2's boot (11:05:44) -- drill 1 ran with the stale suffixed mic target, drill 2 with the corrected name. Drill 1's deafness is fully explained by the stale target on its own; the separately observed missing WirePlumber node is an additional fault, not an established intermittent one. Boot-to-full-green 4m48s stands, dominated by the vLLM multimodal load
  - seeds: `c44`
- `s32` — `challenge pass / unstated-assumptions lens: actuators/sensibo.py + live ac status --json`: the argv lock that makes power-only actuation safe also makes the pod's mode unreachable AND unreadable; observed ambient falling 30.8 to 28 today, which shows the pod happens to be cooling now, but nothing in the system verifies that
  - seeds: `c49`
- `s33` — `challenge pass / lifecycle lens: runtime/listener.py + mode.py`: no revert, restore or `window_end` handling exists anywhere; a hint's effect persists past the window with no decision recorded about whether it should
  - seeds: `c50`
- `s34` — `challenge pass / failure-mode lens: pipeline.py:600 mode resolution`: mode is resolved at decision time and falls back to strict when None, which is safe at window open and unsafe at window close
  - seeds: `c51`
- `s35` — `challenge pass / adjacent-systems + concurrency lens: ask-colleague.sh:195 and the shared gateway on one GB10`: senses, worker and primary lanes share one box; no capacity reservation for senses in this repo or in lobes
  - seeds: `c52`
- `s36` — `challenge pass / reversibility + observability lens: docker-compose.override.yml and .gitignore`: actuation is live via an untracked file while the tracked compose reads dry-run and git status is clean
  - seeds: `c53`
- `s37` — `challenge pass / adjacent-systems lens: Sensibo cloud dependency`: actuation needs internet for the entire window; retry bounds exist in config but the resulting behaviour is unspecified
  - seeds: `c54`
- `s38` — `challenge pass / security lens: web/bind.py + live ss output + tailscale status`: CLEAN against its own contract and the contract holds in practice: the dashboard is bound to loopback and to this host's Tailscale CGNAT address only, never a wildcard or LAN address, exactly as `validate_bind_address` requires. The residual exposure is the DESIGNED one, now concrete: the tailnet holds a phone and another Linux box, so any of them can open the page, press its buttons during a window, and read household transcript text from the in-memory ring. Parked rather than captured, because CLAUDE.md already accepts it
- `s39` — `challenge pass / migration lens: config.py schema and .devague state`: CLEAN and not applicable: the JSON config has no version field and no migration path because nothing persists across restarts by design (invariant 3, no queued action, tmpfs heartbeat). The stale audio node names found today are a config-DRIFT hazard rather than a migration one, already captured as c42
- `s40` — `challenge pass / data-loss lens: privacy surfaces`: CLEAN: audio is never written to disk, transcript text reaches only the local model and the bounded in-memory ring, and logs carry class/intent/verdict/action with pod ids aliased and exceptions reduced to type names. Verified today in practice -- `startup_volume_failed` reported only VolumeCommandError, no message
- `s41` — `challenge pass / adjacent-systems lens: sensibo read + devices against pod POD-ID-REDACTED, and climate-cli`: sensor access is real and rich (ambient, feelsLike, humidity, occupancy, motion, co2, tvoc, iaq, plus two nested room sensors) and outdoor temperature is available from a sibling CLI, but NO read path exposes a MODE or target -- so sensors cannot settle the c49 mode question, and the unused sensing is an opportunity rather than a mitigation. Host and container both run sensibo-cli 0.8.1, so the newer verbs are the same version the image pins
  - seeds: `c56`
- `s42` — `challenge pass / documentation lens: README.md versus ../devague/README.md`: 323 lines and nine sections here against 119 lines and six there; the setup story is an 8-step prose checklist whose operative content is one env var plus a config file, which is exactly the discoverability gap this frame is about -- the README explains where the CLI should report. Load-bearing content that brevity must not cut: the no-hechsher statement, the anyone-in-earshot statement, and the refusal table
  - seeds: `c61`
- `s43` — `challenge pass / documentation lens: shabbos-goy actions output versus grep for a speech tool, plus lobes protocol.py and bluetts_server.py`: the advertised status-to-speech mapping is unimplemented vaporware in the capability table, while the new BlueTTS+phonikud path is rate-compatible at 24 kHz so implementing it would need no playback change -- the gap is entirely the missing call, not the audio format
  - seeds: `c62`
- `s44` — `frame split 2026-09-20, after independent review`: the buildable work left this frame for five new ones: hearing-correctness (c40,c41,c42,c48,c66,c68,c47), readme-refresh (c61,h39,h40), weekday-spoken-status (c62,c63,c64,c65), strict-window-close-boundary (c51, elevated to a requirement), actuation-behaviour-decisions (c50,c54,c57,c58,c59,c60,c37). This frame remains the RECORD of the 2026-09-20 pass -- the deployment, the two reboot drills, the retractions and the provenance -- plus its own subject, the operator verb family and the Qwen eval (c2-c9,c18-c22,c30,c55,c67). The drill evidence is additionally frozen as docs/evidence/2026-09-20-reboot-drills.md, since an evidence log is not spec claims

## Decisions

- user decision 2026-09-20 (answers q1): the fix is a new verb family -- status (up + listening), site (dashboard url/bind, start/inspect), diagnose (aggregates doctor + preflight + heartbeat + connection into one ranked remediation answer) and setup (scaffolds config + env) -- each with an explain-catalog entry, --json and the exit-code contract, so the four example intents map to verb names a Qwen worker guesses
- user decision 2026-09-20 (answers q2): setup is dry-run by default and writes only with --apply, emitting placeholders and never a live key or host, exactly like ac power and volume set
- user decision 2026-09-20 (answers q3): the benchmark is a scripted in-repo eval -- it drives the Qwen3.6-35B-A3B worker lane against the four intents with no bespoke prompt, records the transcript as evidence under docs/, and is re-runnable when the CLI surface changes
- user decision 2026-09-20: the verb surface may be REORGANIZED, not only extended -- an existing verb may be renamed, split or folded in (e.g. listen --healthcheck becoming a status verb, preflight folding into diagnose). Pre-1.0 at 0.10.0, so a breaking rename is allowed, but it must move in lockstep with docker-compose.yml's healthcheck command (docker-compose.yml:117-128), the explain catalog (`shabbos_goy`/explain/catalog.py ENTRIES), overview's verb list, learn, README and the CHANGELOG
- user decision 2026-09-20: close the deafness gap with a CONFIG-GATED knob, `deaf_after_seconds`, defaulting to OFF. When unset the runtime stays byte-identical to the build proven across the two reboot drills; when set, healthcheck additionally requires a transcript stamp within that window, so Compose restarts a deaf listener. Default-off is deliberate: a long genuine silence in an empty room must not cause restarts, and the default behaviour is not to be changed hours before a live window
  - instruction: Implement in `shabbos_goy`/config.py (positive-number validation, None when absent) and `shabbos_goy`/runtime/heartbeat.py's healthcheck (a new named reason code for deafness, e.g. 'deaf'); assert both that unset leaves every existing healthcheck result unchanged and that set-plus-null-transcript returns False
- user decision 2026-09-20: NO further code changes tonight beyond what is already done. Confirmed state: no file under `shabbos_goy`/ was modified -- git status shows only a one-line .gitignore addition plus untracked frame and spec artifacts. Everything that made the box work today was configuration and deployment: corrected audio node names in the private config, a new gitignored docker/shabbos-goy.env, and a gitignored docker-compose.override.yml carrying --apply. The `deaf_after_seconds` knob (c48) is therefore specified but NOT implemented, so the deafness gap stands open through this window with the dashboard as the only detector
- user decision 2026-09-20: the AC's state and mode are DEFINED AS NEEDED out of band -- the household sets the unit to the mode and target it wants (cooling, at whatever temperature) before the window, and the agent's job is only to switch power. This makes the argv lock a feature rather than a gap: excluding --mode by construction is correct, because mode is an operator precondition, not agent state. c49 is therefore not a defect to fix in code but a precondition to state, and c57's open-loop truth stands regardless -- the agent still cannot know the command landed
  - instruction: preflight and the README must STATE this precondition plainly (the pod's mode and target are set by the operator before the window and cannot be verified from this stack), rather than implying the agent manages mode; no code may attempt to set or read mode
- user decision 2026-09-20: connect spoken status to the lobes batch-TTS API rather than withdrawing the advertised capability -- but it is NOT relevant to strict mode and therefore not window-blocking. Strict mode says nothing at all, so a speech path can never fire on Shabbat, Yom Kippur or Yom Tov and cannot touch the core invariant; it is weekday-only work that belongs in the plan. Per c65 the engine, voice and rate are whatever lobes advertises -- BlueTTS with phonikud today -- and this repo neither selects nor defaults them
  - instruction: Implement as a weekday-only path calling POST /v1/audio/speech through the gateway, reusing `PLAYBACK_SAMPLE_RATE_DEFAULT`=24000 (already correct for both engines). Until it exists, 'shabbos-goy actions' must not advertise it as present.

## Hard questions

- does 'easy to set up' mean new CLI verbs (a status/site/setup/diagnose family) or only better explain-catalog + README copy over the verbs that exist?
- may a setup verb WRITE -- scaffold $`XDG_CONFIG_HOME`/shabbos-goy/config.json and an env template -- or must it stay read-only, given every actuating verb is dry-run by default and --apply actuates?
- is the unaided-Qwen benchmark an in-repo repeatable eval (a scripted worker run whose transcript lands as evidence) or a manual drill the operator runs once?

## Open parks

- [unknown_nonblocking] every tailnet device -- today an iPhone and an orin, both of which leave the house -- can open the dashboard with no token, press its controls during a window, and read household transcript text from the in-memory ring. CLAUDE.md accepts this deliberately (tailnet membership is the authentication), so it is residual risk rather than a defect, but it has never been stated as this concrete
- [unknown_nonblocking] whether the 15s `strict_mode_delay` actually satisfies whatever the deliberate-delay premise is for -- docs/halacha-open-questions.md parks the delay question itself, and nothing in the frame says what value would answer it or who decides
- [unknown_nonblocking] actuation success is unverifiable in the moment and only inferable over minutes from a thermal trend, so within any single decision the agent cannot know whether it acted. Whether that matters halachically -- an action taken that may not have taken effect, or a hint declined because of a stale belief -- belongs with the premises in docs/halacha-open-questions.md, not here
- [follow_up] run the golden set against this box's real room acoustics -- the audio-batch and audio-realtime entrances through the reSpeaker in the actual room with the AC running -- and record whether `hard_false_positives_strict` stays 0 there. Until then the 295-row bound is evidence about typed and synthesised input only

## Resolved vagueness

- [unknown_blocking] the residual surprise risk this pass could not reduce: T2's labelling bound is 295 typed and synthesised rows, while tonight is live household Hebrew from multiple speakers through a 4-mic array in a room with a running AC. The pass examined the code paths, not the acoustics -- no golden-set run has ever been done against this box's real room — resolved: user direction 2026-09-20: accepted and carried knowingly, not retired. The golden set has never been run against this box's real room acoustics -- live household Hebrew, several speakers, a 4-mic array, a running AC -- so T2's 295-row bound is not evidence about THIS room, and tonight proceeds without that measurement. Nothing may claim the labelling bound holds here until the run exists
- [unknown_blocking] the eval has no rubric: what tools the worker holds (a raw shell, or the ask-colleague harness whose own scaffolding is Claude-authored prompt text), what 'resolved' means for an intent like 'why is it not working' that has no exit-0 end state, whether --help counts against the command budget k, and who judges. Until these are fixed the eval is not buildable and T3 is not testable — resolved: the eval rubric is PLAN-side work: tools available to the worker, what counts as resolved per intent, whether --help counts against k, and who judges are all task acceptance criteria for /spec-to-plan, not spec claims. T3 stays untestable until the plan defines them, and nothing may report an eval result before it does
